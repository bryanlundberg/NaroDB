{
  "compilerOptions": {
    "allowJs": true,
    "checkJs": true,
    "target": "ES2020",                      // Versión de JavaScript a generar
    "module": "ESNext",                      // Usa módulos de ESNext
    "lib": ["ESNext"],                       // Librerías estándar incluidas
    "moduleResolution": "Node",             // Resolución de módulos estilo Node.js
    "outDir": "./dist",                      // Carpeta de salida de compilación
    "rootDir": "./src",                      // Carpeta raíz del proyecto
    "declaration": true,                     // Genera archivos .d.ts
    "strict": true,                          // Activa todas las comprobaciones estrictas
    "esModuleInterop": true,                 // Interoperabilidad con CommonJS
    "skipLibCheck": true,                    // Omite la comprobación de tipos en .d.ts externos
    "forceConsistentCasingInFileNames": true // Fuerza consistencia de casos en nombres de archivo
  },
  "include": [
    "src/**/*"                               // Archivos incluidos (código fuente)
  ],
  "exclude": [
    "node_modules",                          // Exclusiones comunes
    "dist",                                  // Carpeta de salida
    "**/*.test.ts",                          // Test files
    "**/*.test.js"
  ]
}
